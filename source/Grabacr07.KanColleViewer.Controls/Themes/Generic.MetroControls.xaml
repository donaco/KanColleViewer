<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
                    xmlns:metro="clr-namespace:Grabacr07.KanColleViewer.Controls.Metro">

    <!-- Phase 4: ウィンドウ系コントロールの内製テンプレート -->

    <!-- ── アイコン (MetroRadiance/Styles/Icons.xaml から移植) ────────── -->

    <Style x:Key="ChromeIconBaseKey" TargetType="{x:Type Path}">
        <Setter Property="Width"   Value="12" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Fill"
                Value="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentControl}}, Path=Foreground}" />
    </Style>

    <Style x:Key="MinimizeIconKey" TargetType="{x:Type Path}" BasedOn="{StaticResource ChromeIconBaseKey}">
        <Setter Property="Width"             Value="10" />
        <Setter Property="Height"            Value="9" />
        <Setter Property="Data"              Value="M5,30 L25,30 L25,35 L5,35 z" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
    </Style>

    <Style x:Key="MaximizeIconKey" TargetType="{x:Type Path}" BasedOn="{StaticResource ChromeIconBaseKey}">
        <Setter Property="Width"  Value="11" />
        <Setter Property="Height" Value="11" />
        <Setter Property="Data"   Value="M5,12.5 L5,32.5 L30.078125,32.5 L30.078125,12.5 z M2.5,7.5 L32.578125,7.5 L32.578125,35 L2.5,35 z" />
    </Style>

    <Style x:Key="NormalizeIconKey" TargetType="{x:Type Path}" BasedOn="{StaticResource ChromeIconBaseKey}">
        <Setter Property="Data" Value="M5,22.5 L5,32.5 L22.5,32.5 L22.5,22.5 z M12.5,12.5 L12.5,17.5 L25,17.5 L25,22.5 L30,22.5 L30,12.5 z M10,7.5 L32.5,7.5 L32.5,25 L25,25 L25,35 L2.5,35 L2.5,17.5 L10,17.5 z" />
    </Style>

    <Style x:Key="CloseIconKey" TargetType="{x:Type Path}" BasedOn="{StaticResource ChromeIconBaseKey}">
        <Setter Property="Width"  Value="10" />
        <Setter Property="Height" Value="10" />
        <Setter Property="Data"   Value="M48.578243,0 L57.856652,0 71.665367,15.433628 85.779,0 94.404,0 75.760063,19.752212 94.404,39.999999 85.125595,39.999999 71.229759,24.460176 56.98544,39.999999 48.404,39.999999 67.135063,20.141593 z" />
    </Style>

    <!-- ══════════════════════════════════════════════════════
         MetroWindow
    ══════════════════════════════════════════════════════ -->
    <Style TargetType="{x:Type metro:MetroWindow}">
        <Setter Property="Background"         Value="{DynamicResource ThemeBrushKey}" />
        <Setter Property="Foreground"         Value="{DynamicResource ActiveForegroundBrushKey}" />
        <Setter Property="BorderBrush"        Value="{DynamicResource AccentBrushKey}" />
        <Setter Property="BorderThickness"    Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding"  Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type metro:MetroWindow}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            UseLayoutRounding="{TemplateBinding UseLayoutRounding}">
                        <AdornerDecorator>
                            <Grid>
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                                <!-- リサイズグリップ -->
                                <Border x:Name="PART_ResizeGrip"
                                        VerticalAlignment="Bottom"
                                        HorizontalAlignment="Right"
                                        Background="Transparent"
                                        Width="16" Height="16" />
                            </Grid>
                        </AdornerDecorator>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding"         Value="8" />
                        </Trigger>
                        <Trigger Property="IsActive" Value="False">
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushKey}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══════════════════════════════════════════════════════
         CaptionButton
    ══════════════════════════════════════════════════════ -->
    <Style TargetType="{x:Type metro:CaptionButton}">
        <Setter Property="Width"                    Value="34" />
        <Setter Property="Height"                   Value="26" />
        <Setter Property="Background"               Value="Transparent" />
        <Setter Property="Foreground"               Value="{DynamicResource ForegroundBrushKey}" />
        <Setter Property="BorderThickness"          Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment"   Value="Center" />
        <Setter Property="SnapsToDevicePixels"      Value="True" />
        <Setter Property="FocusVisualStyle"         Value="{x:Null}" />
        <Setter Property="IsTabStop"                Value="False" />
        <Setter Property="Focusable"                Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type metro:CaptionButton}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Grid Margin="{TemplateBinding Padding}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter x:Name="contentPresenter" />
                            <Path x:Name="icon"
                                  Fill="{TemplateBinding Foreground}"
                                  Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowAction" Value="Minimize">
                            <Setter TargetName="icon" Property="Style" Value="{StaticResource MinimizeIconKey}" />
                        </Trigger>
                        <Trigger Property="WindowAction" Value="Maximize">
                            <Setter TargetName="icon" Property="Style" Value="{StaticResource MaximizeIconKey}" />
                        </Trigger>
                        <Trigger Property="WindowAction" Value="Normalize">
                            <Setter TargetName="icon" Property="Style" Value="{StaticResource NormalizeIconKey}" />
                        </Trigger>
                        <Trigger Property="WindowAction" Value="Close">
                            <Setter TargetName="icon" Property="Style" Value="{StaticResource CloseIconKey}" />
                        </Trigger>
                        <!-- コンテンツがなければアイコンを表示 -->
                        <Trigger Property="Content" Value="{x:Null}">
                            <Setter TargetName="icon" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource ActiveBackgroundBrushKey}" />
                            <Setter Property="Foreground" Value="{DynamicResource ActiveForegroundBrushKey}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrushKey}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrushKey}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══════════════════════════════════════════════════════
         SystemButtons (最小化・最大化・閉じる の 3ボタンセット)
    ══════════════════════════════════════════════════════ -->
    <Style TargetType="{x:Type metro:SystemButtons}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type metro:SystemButtons}">
                    <StackPanel Orientation="Horizontal">
                        <metro:CaptionButton WindowAction="Minimize" />
                        <metro:CaptionButton WindowAction="Normalize" />
                        <metro:CaptionButton WindowAction="Maximize" />
                        <metro:CaptionButton WindowAction="Close" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══════════════════════════════════════════════════════
         TabView
    ══════════════════════════════════════════════════════ -->
    <Style TargetType="{x:Type metro:TabView}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type metro:TabView}">
                    <StackPanel Background="{TemplateBinding Background}"
                                IsItemsHost="False">
                        <ItemsPresenter />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Foreground"       Value="{DynamicResource ActiveForegroundBrushKey}" />
                    <Setter Property="Height"           Value="38" />
                    <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static SystemParameters.FocusVisualStyleKey}}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentHighlightBrushKey}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrushKey}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentBrushKey}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrushKey}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══════════════════════════════════════════════════════
         ResizeGrip
    ══════════════════════════════════════════════════════ -->
    <Style TargetType="{x:Type metro:ResizeGrip}">
        <Setter Property="Width"              Value="16" />
        <Setter Property="Height"             Value="16" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment"  Value="Bottom" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type metro:ResizeGrip}">
                    <Grid Width="16" Height="16" Margin="1">
                        <!-- ドット3×3パターン -->
                        <Path Fill="{DynamicResource ForegroundBrushKey}"
                              Data="M 10,6 L 12,6 L 12,8 L 10,8 Z
                                    M 6,10 L 8,10 L 8,12 L 6,12 Z
                                    M 10,10 L 12,10 L 12,12 L 10,12 Z
                                    M 6,14 L 8,14 L 8,16 L 6,16 Z
                                    M 10,14 L 12,14 L 12,16 L 10,16 Z
                                    M 14,14 L 16,14 L 16,16 L 14,16 Z" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
